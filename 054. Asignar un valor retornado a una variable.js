/*
Asignar un valor retornado a una variable.
*/

function sumar(a, b) {
  return a + b;
}

// Se asigna el valor retornado a una variable 
let resultado = sumar(5, 3);
console.log(resultado);

// Ejemplo

function objetivo(a) {
  return "Mi meta es aprender " + a;
}

let programar = objetivo("JavaScript");
console.log(programar);
